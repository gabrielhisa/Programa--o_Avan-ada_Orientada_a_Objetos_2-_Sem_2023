package com.estudante.estudante.repository;

public class ProfessorRepositoryImplementacao {

    // Adicionar > implements ProfessorRepository
    // Adicionar @Service sobre a classe

    /*
    private final EntityManager entityManager;

    @Autowired
    public ProfessorRepositoryImplementacao(EntityManager entityManager) {
        this.entityManager = entityManager;
    }

    @Override
    public List<Professor> findAll() {
        TypedQuery<Professor> query = entityManager.createQuery("SELECT p FROM Professor p", Professor.class);
        return query.getResultList();
    }
    */

}
