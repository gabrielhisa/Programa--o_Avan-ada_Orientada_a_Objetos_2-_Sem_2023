package com.estudante.estudante.repository;

public class CursoRepositoryImplementacao {

    // Adicionar > implements CursoRepository
    // Adicionar @Service sobre a classe

    /*
    private final EntityManager entityManager;

    @Autowired
    public CursoRepositoryImplementacao(EntityManager entityManager) {
        this.entityManager = entityManager;
    }

    @Override
    public List<Curso> findAll() {
        TypedQuery<Curso> query = entityManager.createQuery("SELECT c FROM Curso c", Curso.class);
        return query.getResultList();
    }
    */

}
