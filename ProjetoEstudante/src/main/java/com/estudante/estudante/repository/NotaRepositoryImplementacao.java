package com.estudante.estudante.repository;

public class NotaRepositoryImplementacao {

    // Adicionar > implements NotaRepository
    // Adicionar @Service sobre a classe

    /*
    private final EntityManager entityManager;

    @Autowired
    public NotaRepositoryImplementacao(EntityManager entityManager) {
        this.entityManager = entityManager;
    }

    @Override
    public List<Nota> findAll() {
        TypedQuery<Nota> query = entityManager.createQuery("SELECT n FROM Nota n", Nota.class);
        return query.getResultList();
    }
    */

}
